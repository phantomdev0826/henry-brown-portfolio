/**
 * useOnScreen - React hook to determine if an element is visible in the viewport.
 * @param ref - React ref to the DOM element
 * @param rootMargin - Optional margin around the root
 * @returns boolean - true if element is on screen
 */
 